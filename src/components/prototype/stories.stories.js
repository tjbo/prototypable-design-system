// import Icon from '../icon'
import theme from '../../theme'
import Grid from '../grid'
import Icon from '../icon'

export default {
  title: 'Thing',
  component: Grid,
}
export const Default = () => {
  return (
    <div>
      <Icon name="yahoo-finance" boxSize={24} />
    </div>
  )
}
